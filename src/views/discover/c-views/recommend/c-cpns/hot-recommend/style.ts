import styled from "styled-components"

export const HotRecommandWrapper = styled.div`
  .hot-recommend-list {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    cursor: pointer;
  }
`
